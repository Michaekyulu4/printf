#include "main.h"
/**
 * putstr - print string
 * @c: string
 * Return: number of bytes
 */
int putstr(char *c)
{
	int count = 0;

	if (c)
	{
		for (count = 0; c[count] != '\0'; count++)
		{
			inputch(c[count]);
		}
	}
	return (count);
}
